<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AdoGenerator</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="<%= root_path %>">AdoGenerator</a>
        <div class="navbar-nav">
          <%= link_to "All Record History", history_calendar_path, class: "nav-link" %>
        </div>
        <div class="collapse navbar-collapse">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <a class="nav-link" href="<%= records_path %>">Records</a>
            </li>
            <% if user_signed_in? && current_user.admin? %>
              <li class="nav-item">
                <a class="nav-link" href="<%= new_record_path %>">New Record</a>
              </li>
            <% end %>
          </ul>
          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
              <li class="nav-item d-flex flex-column align-items-start">
                <span class="nav-link "> Signed in as <%= current_user.email %><strong>/<%= current_user.role.capitalize %></strong></span>
              </li>
              <li class="nav-item d-flex align-items-center">
                <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-primary btn-sm ms-2" %>
              </li>
              <li class="nav-item d-flex align-items-center ms-3">
                <button type="button" class="btn btn-outline-secondary btn-sm" onclick="toggleAdminForm()">
                  🔒
                </button>
                <div id="admin-form" class="d-none ms-2">
                  <%= form_with url: admin_role_path, method: :put, local: true, class: "d-flex" do |f| %>
                    <%= f.password_field :catchphrase, placeholder: "Enter Catchphrase", class: "form-control form-control-sm me-2", style: "width: 150px;" %>
                    <%= f.submit "Apply", class: "btn btn-outline-dark btn-sm" %>
                  <% end %>
                </div>
              </li>
            <% end %>
            <script>
              function toggleAdminForm() {
                var form = document.getElementById('admin-form');
                form.classList.toggle('d-none');
              }
            </script>
          </ul>
        </div>
      </div>
    </nav>
    <div class="container mt-4">
      <% if notice %>
        <div class="alert alert-success"><%= notice %></div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger"><%= alert %></div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
