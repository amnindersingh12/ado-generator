<!DOCTYPE html>
<html lang="en" data-bs-theme="light">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AdoGenerator - Your Attendance Hub</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload', defer: true %>

  <%# Correctly link to your downloaded Bootstrap and Bootstrap Icons CSS %>
  <%= stylesheet_link_tag 'bootstrap.min', media: 'all', 'data-turbo-track': 'reload' %>

  <%# Link to your downloaded Chart.js and Bootstrap Bundle JS (using asset_path is fine for JS) %>
  <script src="<%= asset_path 'chart.umd.js' %>" defer></script>
  <script src="<%= asset_path 'bootstrap.bundle.min.js' %>" defer></script>


  <style>
    /* Modern UI & Elegant Tone - Base Styles with Theme Variables */
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* A clean, modern font */
      background-color: var(--body-bg); /* Use theme variable */
      color: var(--body-color); /* Use theme variable */
      line-height: 1.6;
      min-height: 100vh; /* Ensure full viewport height */
      transition: background-color 0.3s ease, color 0.3s ease; /* Smooth theme transition */
    }

    .content-area {
      padding: 2rem;
      flex-grow: 1;
    }

    .alert {
      margin-bottom: 1.5rem;
      border-radius: 0.3rem;
    }

    /* Define default light theme variables */
    :root {
      --body-bg: #f8f9fa; /* Light grey background */
      --body-color: #343a40; /* Dark grey text */
    }

    /* Dark theme variables (will be applied when the body has a 'dark-theme' class) */
    body.dark-theme {
      --body-bg: #212529; /* Dark background */
      --body-color: #f8f9fa; /* Light text */
    }

    /* Styles for elements within the content area will inherit body text color */
    h1, h2, h3, h4, h5, h6, p, strong, em, span, a {
      color: inherit; /* Inherit text color from the body */
    }

    /* Ensure Bootstrap components also adapt (some might need overrides) */
    .bg-light {
      background-color: var(--body-bg) !important;
    }
    .text-light {
      color: var(--body-color) !important;
    }
    .btn-outline-light {
      color: var(--body-color);
      border-color: var(--body-color);
    }
    .btn-outline-light:hover {
      background-color: var(--body-color);
      color: var(--body-bg);
    }
    .dropdown-menu-dark {
      background-color: #343a40; /* Dark dropdown background */
      color: #f8f9fa;
    }
    .dropdown-item {
      color: #f8f9fa;
    }
    .dropdown-item:hover {
      background-color: #495057;
    }
    .nav-link {
      color: inherit; /* Inherit text color */
    }
    .form-control {
      background-color: #fff;
      color: #343a40;
      border: 1px solid #ced4da;
    }
    .form-control:focus {
      border-color: #007bff;
      box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
    }
    body.dark-theme .form-control {
      background-color: #495057;
      color: #e9ecef;
      border-color: #6c757d;
    }
    body.dark-theme .form-control:focus {
      border-color: #6610f2;
      box-shadow: 0 0 0 0.2rem rgba(102, 16, 242, 0.25);
    }
    .card {
      background-color: #fff;
      color: #343a40;
      border: 1px solid rgba(0, 0, 0, 0.125);
    }
    body.dark-theme .card {
      background-color: #343a40;
      color: #e9ecef;
      border-color: #6c757d;
    }

    /* You can add more overrides for other Bootstrap components as needed */
  </style>
</head>
<body>
  <div class="d-flex">
    <%= render 'shared/navbar' %>
    <div class="content-area flex-grow-1">
      <div class="p-4">
        <% if notice %>
          <div class="alert alert-success"><%= notice %></div>
        <% end %>
        <% if alert %>
          <div class="alert alert-danger"><%= alert %></div>
        <% end %>
        <%= yield %>
      </div>
    </div>
  </div>
</body>
</html>
