<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Attendance Report</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
      .user-photo {
        width: 120px;
        height: 120px;
        object-fit: cover;
        border: 2px solid #000;
        border-radius: 8px;
        background-color: #f8f9fa;
      }
      .signature-box {
        border: 2px solid #000;
        padding: 20px;
        height: 120px;
        display: flex;
        flex-direction: column;
        justify-content: flex-end;
        text-align: center;
      }
      .signature-space {
        border-top: 2px solid #000;
        margin-top: auto;
        margin-bottom: 10px;
      }
      @media print {
        .user-photo, .table, .table th, .table td, .signature-box {
          border-color: #000 !important;
          -webkit-print-color-adjust: exact;
          print-color-adjust: exact;
        }
      }
    </style>
  </head>
  <body>
    <div class="container my-4 p-4 border border-dark">
      <h2 class="text-center text-uppercase mb-4 "> Attendance Report</h2>
      <!-- User Info Section -->
      <div class="row mb-4 align-items-start">
        <div class="col-md-3 text-center">
          <% if @record.photo.attached? %>
            <%= base64_image_tag(@record.photo, width: 200, height: 200, class: "rounded") %>
          <% else %>
            <div class="user-photo d-flex align-items-center justify-content-center text-muted">No Photo</div>
          <% end %>
        </div>
        <div class="col-md-9">
          <p><strong>Name:</strong> <%= @record.name || "N/A" %></p>
          <p><strong>Age:</strong> <%= @record.date_of_birth.present? ? "#{Time.zone.now.year - @record.date_of_birth.year} years" : "N/A" %></p>
          <p><strong>Father's Name:</strong> <%= @record.father_name.presence || "N/A" %></p>
          <p><strong>Contact Number:</strong> <%= @record.contact_number %></p>
          <p><strong>Address:</strong> <%= [@record.address, @record.city, @record.state, @record.pincode].compact.join(', ') %></p>
          <p><strong>Government ID Number:</strong> <%= @record.government_id_number.presence || "N/A" %></p>
        </div>
      </div>
      <!-- Attendance Table -->
      <h5 class="fw-bold mb-3">Attendance Entries:</h5>

        <% if @record.attendances.count > 0 %>


      <table class="table table-striped table-bordered">
        <thead>
          <tr>
            <th>#</th>
            <th>Check-In Time</th>
            <th>Check-Out Time</th>
            <th>Marked By</th>
          </tr>
        </thead>
        <tbody>
          <% @attendances.each_with_index do |attendance, index| %>
            <tr>
              <td><%= index + 1 %></td>
              <td><%= format_datetime(attendance.in_time) %></td>
              <td><%= attendance.out_time.present? ? format_datetime(attendance.out_time) : "N/A" %></td>
              <td><%= attendance.user&.email || "N/A" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <% else %>
<p class="text-white bg-dark">No Record Found</p>
      <% end %>
      <!-- Signature Section -->
      <div class="row mt-5">
        <p class="fw-bold mb-0">Signature of Issuing Officer</p>
      </div>
      <br>
      <br>
      <div>
        <p class="fw-bold mb-0">Signature of User</p>
      </div>
    </div>
  </div>
  <!-- Footer -->
  <div class="text-center mt-4 text-muted small">
    (With Stamp)<br>
    <br>
    This is a system-generated report. Please ensure all information is verified before submission.<br>
    [Page 1]
  </div>
</div>
</body>
</html>
